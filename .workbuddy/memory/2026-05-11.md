# 2026-05-11 工作记录

## 修复三个问题

### 1. 删除根目录错放的 zh-TW 目录
- 根目录 `F:\26年4月\kb01\zh-TW\` 是完整项目副本（误放位置）
- 正确位置是 `F:\26年4月\kb01\pages\zh-TW\`（362个HTML文件）
- 已安全删除根目录的错放副本

### 2. 修复首页繁体按钮样式（与菜单区分）
- `pages/styles.css` 和 `pages/zh-TW/styles.css` 同步修复
- 移除 `margin-top:8px`，按钮不再偏下
- 保留金色 pill 背景，与顶部菜单（纯文字链接）形成区分

### 3. 修复繁体版"简"字偏下 + 图标偏大
- 移除 `margin-top:8px` → "简" 字垂直居中对齐
- `font-size`: 13px → 12px（文字缩小）
- `height/line-height`: 28px → 24px（按钮缩小）
- `padding`: 0 14px → 0 10px（横向间距缩小）
- `border-radius`: 14px → 12px（略小圆角）

## 技术细节
- CSS 文件为 minified 格式（单行），使用 PowerShell 替换字符串
- 两个 CSS 文件需同步：`pages/styles.css` 和 `pages/zh-TW/styles.css`

## 修复移动端侧边栏问题（2026-05-11 补充）

### 问题诊断
- HTML 文件中调用了 `toggleSidebar()` 函数（onclick="toggleSidebar()"）
- 但 `dist/` 目录的 HTML 文件没有引用 `app.js`（该函数定义在 `pages/app.js`）
- 导致移动端点击菜单按钮无反应

### 修复步骤
1. 复制 `pages/app.js` 到 `dist/app.js`
2. 创建 Python 脚本 `add_appjs_reference.py`，给 `dist/` 目录所有 HTML 文件添加 `<script src="/app.js"></script>` 引用
3. 运行脚本，成功修复 47 个 HTML 文件
4. 清理临时文件（`sidebar.js`、`add_appjs_reference.py`）

### 验证
- `dist/index.html` 等文件现在都引用了 `app.js`
- `toggleSidebar()` 函数已正确定义（在 `app.js` 第74-86行）
- 移动端侧边栏应该能正常切换了

## 清理 Vercel 旧部署（2026-05-11）

- 任务：删除 ramana-kb 项目 36 小时以前的旧部署
- 本次新删除 18 个（7 个 kb01-* + 11 个 20260410104230-*）
- 累计跨两次会话共删除约 36 个旧部署
- 验证结果：当前 12 个部署全部为 36h 内，无旧部署残留 ✓
- 截止时间戳：1778332891147（2025-05-05 14:41:31 UTC 36h前）

## 统计全站简体/繁体页面数量（2026-05-11 补充）

### 统计结果
- **简体版**（dist/）：47 个 HTML 文件
- **繁体版**（dist/zh-TW/）：0 个 HTML 文件（尚未构建）
- **源代码目录**（pages/）：
  - 简体：362 个 HTML 文件
  - 繁体：362 个 HTML 文件（pages/zh-TW/）

### 说明
- `dist/` 目录是构建输出目录，包含 47 个简体 HTML 文件
- `pages/zh-TW/` 目录包含 362 个繁体 HTML 源代码文件，但尚未构建到 `dist/zh-TW/`
- 可能需要运行构建命令来生成繁体版页面

## 修复繁简切换按钮大小（2026-05-11 傍晚）

### 问题
- 用户反馈繁体页面的简体按钮太大，简体页面的繁体按钮太小
- 两个按钮都使用 `.lang-toggle` 类

### 修复
- `pages/styles.css` 第1608-1617行
- 调整为适中大小：height: 20px, line-height: 20px, font-size: 10px, padding: 0 6px
- 两个页面的按钮现在大小一致
